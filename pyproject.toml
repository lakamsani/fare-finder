[project]
name = "fare-finder"
version = "1.0.0"
description = "CLI tool for finding the cheapest one-way flights between US cities"
requires-python = ">=3.10"

[project.scripts]
fare-finder = "fare_finder.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
